# ── existing stack (pinned to what was already verified working) ───────────
fastapi==0.138.0
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2
requests==2.34.2
google-generativeai==0.8.6

# ── agentic AI stack (LangChain + LangGraph + LangSmith + Gemini) ─────────
# Left un-pinned to a specific patch so `pip install -r requirements.txt`
# always resolves the latest mutually-compatible set; run
# `pip freeze > requirements.lock.txt` once your env is working if you want
# a reproducible pin for submission/demo day.
langchain>=0.3
langchain-core>=0.3
langchain-google-genai>=2.0
langgraph>=0.2
langgraph-checkpoint>=2.0
langsmith>=0.1

# ── database ────────────────────────────────────────────────────────────
firebase-admin>=6.5

# ── pattern analysis ────────────────────────────────────────────────────
pandas>=2.2

# ── file uploads (multipart form parsing, if you add REST upload routes) ──
python-multipart>=0.0.9
